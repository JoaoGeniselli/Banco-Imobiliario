name: New code validation

on:
  pull_request:
    types: [ opened, synchronize, reopened ]
    branches:
      - develop
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:


